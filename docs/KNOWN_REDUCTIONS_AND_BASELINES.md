# Known Reductions And Baselines

Status: public reduction and baseline map.

Purpose: prevent novelty inflation by recording what nearby mathematical and
empirical frameworks already cover, and what a stronger Omega / Layer A claim
would have to witness before being treated as more than a promising synthesis.

Claim boundary:

```text
This document does not establish novelty.
It does not validate Omega.
It does not define or detect value, valuers, agency, identity, selfhood,
compatibility, boundary, support, capture, or erasure.
```

## Core Object Under Pressure

The current lower object can be described as:

```text
Layer A:
  consequence-bearing distinctions under admissible continuation.

It asks which distinctions are preserved, erased, recoverable, transformed,
composed, or made non-neutral by continuation under declared observations,
comparisons, and recovery contracts.
```

This object is not licensed by vocabulary. It is licensed only when a concrete
witness separates it from simpler baselines under declared controls.

Until at least a small battery of such witnesses exists, Layer A should be
described as a promising synthesis and measurement discipline, not as a
demonstrated new science.

## Baseline Map

| Baseline | What it already covers | What Layer A must not pretend is new | Required witness pressure |
| --- | --- | --- | --- |
| Reachability / frontier size | Possible destinations, paths, supports, or frontier counts. | "What can be reached." | Same reachability or frontier size, different declared recoverability or consequence-bearing distinctions. |
| Entropy | Uncertainty, spread, dispersion, and distributional breadth. | Randomness or dispersion. | Same entropy, different recoverable distinction structure, or same entropy with one continuation preserving a declared distinction and another erasing it. |
| Mutual information / channel capacity | Statistical dependence, transmitted information, and optimal channel use. | Amount of recoverable information in the generic information-theoretic sense. | Same mutual information or capacity, different declared registry recovery, provenance, or theorem-transfer eligibility. |
| Observability / controllability | Reconstructing state from outputs and steering states through controls. | System identification or control reach. | Same observability rank or control reach, different consequence merge-block profile or recovery provenance. |
| Bisimulation / partition refinement | Behavioral equivalence under transitions and observations. | Quotienting by behavior. | Either show Layer A reduces to bisimulation in a special case, or give same bisimulation partition under one observation panel with different consequence profiles under a declared expanded panel. |
| Causal abstraction / intervention | What changes under intervention and abstraction maps. | Causal effect, invariance, or intervention sensitivity. | Same intervention summary, different recoverability after continuation, or different provenance for the claimed recovery. |
| Viability kernels | Staying inside constraint sets under dynamics. | Persistence inside an admissible set. | Same viability kernel size, different recoverable distinctions or irreversible-loss profile. |
| Markov / channel theory | Transition kernels, supports, decoding, and stochastic channel behavior. | Kernels, transition probabilities, decoder success, or support recovery. | Same kernel-level aggregate or Bayes-best success, different declared-registry recovery or theorem-transfer class. |
| MDL / compression | Concise representation and model-description tradeoffs. | Compression or short description. | Same description length or compression score, different merge soundness or consequence-separated pairs. |
| Spectral / mixing metrics | Connectivity, diffusion, relaxation, and mixing behavior. | Graph or dynamics spectral behavior. | Same spectral gap or mixing metric, different distinction preservation or recovery profile. |
| Formal verification / temporal logic | Property satisfaction over traces. | Model checking or trace-property satisfaction. | Same satisfied property set under a coarse specification, different consequence-bearing distinctions under declared contexts. |

## Reduction Cases We Should Welcome

Some adapters may reduce cleanly to known frameworks. That is not a failure.
It is part of the reduction map.

Examples:

```text
Exact deterministic transition plus observation equivalence may reduce to
bisimulation.

A finite channel with fixed source/target observations and a declared decoder
may reduce to channel decoding or support recovery.

Viability with a single declared constraint may reduce to viability-kernel
membership.
```

The novelty burden lives where the current framework distinguishes a structure
that a baseline collapses, or where it forces a provenance, declaration, or
claim-boundary discipline that the baseline result by itself does not supply.

## Required Witness Classes

The next credibility target is a small suite of baseline-controlled witnesses.
Each witness should name the controlled baseline, output a retained fixture, and
state what it does and does not support.

### Same Reachability, Different Recoverability

Purpose: show that reachable support or frontier count is insufficient for
declared consequence-bearing recovery.

Minimal substrate: finite state/channel pair over the same carrier.

Baselines controlled: reachable support, reachable count, or frontier size.

Expected artifact: CSV/JSON fixture plus a test or smoke command.

Claim supported: reachability count alone does not determine declared
recoverability.

Claim not supported: value, Omega, agency, identity, or valuerhood.

### Same Entropy, Different Consequence-Bearing Structure

Purpose: show that distributional spread is insufficient for distinction
preservation.

Minimal substrate: paired finite distributions or channels with equal entropy.

Baselines controlled: entropy and carrier size.

Expected artifact: fixture with declared distinctions and pass/fail recovery
checks.

Claim supported: entropy alone does not determine which declared distinctions
are preserved or erased.

Claim not supported: that lower entropy or higher entropy is better.

### Same Mutual Information Or Capacity, Different Declared Recovery

Purpose: separate generic information transfer from declared registry recovery.

Minimal substrate: finite stochastic channels with matched information metric.

Baselines controlled: mutual information, capacity, or Bayes-best recovery.

Expected artifact: retained registry run with provenance and adversarial audit.

Claim supported: generic information transfer does not automatically license
the declared instrument's recovery claim.

Claim not supported: semantic recovery or substrate-general recovery.

### Same Bisimulation Under One Panel, Different Consequence Profile Under Another

Purpose: show where the framework is panel-sensitive rather than a hidden
global identity claim.

Minimal substrate: finite transition system with two declared observation or
continuation panels.

Baselines controlled: bisimulation partition under the first panel.

Expected artifact: two panel specs and exact consequence-profile comparison.

Claim supported: panel declaration changes the admissible merge question.

Claim not supported: arbitrary post-hoc panel selection.

### Same Pairwise Compatibility, Different Joint Compatibility

Purpose: block the move from pairwise checks to class-level or family-level
claims.

Minimal substrate: finite triple with pairwise-compatible links and a joint
failure.

Baselines controlled: pairwise compatibility count.

Expected artifact: Lean theorem or finite fixture.

Claim supported: chain or pairwise evidence is not enough for joint
compatibility.

Claim not supported: any particular empirical class is meaningful.

### Same Frontier Morphology, Different Declared Loss Profile

Purpose: keep Future Field Atlas morphology subordinate to consequence and
loss questions.

Minimal substrate: paired finite dynamics with matched reachable-frontier
summary statistics.

Baselines controlled: frontier size, rank, or morphology feature set.

Expected artifact: retained run comparing frontier features and declared
horizon-local loss markers.

Claim supported: morphology alone is insufficient for the stronger trajectory
or loss claim.

Claim not supported: valuerhood, capture, support, or Omega validation.

### Same Compression Score, Different Merge Soundness

Purpose: prevent concise abstractions from becoming ontological objects by
default.

Minimal substrate: finite consequence system with two abstractions of matched
description length.

Baselines controlled: description length or compression score.

Expected artifact: exact profile plus soundness/completeness audit.

Claim supported: compression score alone does not imply merge soundness.

Claim not supported: that less compression or more compression is preferred.

## First Retained Witnesses

The first retained targets are:

```text
same reachability, different recoverability
same entropy, different declared recovery profile
same mutual information or capacity, different declared registry recovery
same optimized recovery success, different declared recovery
same marginal Bayes-best success, different joint Bayes-best success
same compression score, different merge soundness
same frontier morphology, different declared horizon-local loss profile
```

Retained notes:

```text
docs/research_notes/validation_results/baseline_witnesses/same_reachability_different_recovery_v0.md
docs/research_notes/validation_results/baseline_witnesses/same_entropy_different_recovery_profile_v0.md
docs/research_notes/validation_results/baseline_witnesses/same_mutual_information_different_declared_recovery_v0.md
docs/research_notes/validation_results/baseline_witnesses/same_optimized_success_different_declared_recovery_v0.md
docs/research_notes/validation_results/baseline_witnesses/same_marginal_success_different_joint_success_v0.md
docs/research_notes/validation_results/baseline_witnesses/same_compression_score_different_merge_soundness_v0.md
docs/research_notes/validation_results/baseline_witnesses/same_frontier_morphology_different_loss_profile_v0.md
```

One-command reproduction:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\validation\run_baseline_witness_smoke.ps1
```

This reruns all seven witnesses into `.tmp`, checks regenerated summary digests
against the committed retained summaries, and runs the focused witness tests.
See [BASELINE_WITNESS_SMOKE.md](BASELINE_WITNESS_SMOKE.md).

Implemented reachability construction:

```text
Use a finite carrier and two finite channels or continuations.
Match reachable target support or frontier size.
Declare a distinction and a recovery contract before scoring.
Construct one channel that preserves the declared distinction under the
contract and one that erases it, while preserving the reachability baseline.
Emit a retained fixture and a testable pass/fail result.
```

Parameterized family check:

```text
For k = 1..5 nuisance bits, compare a channel whose target support is keyed by
declared d against channels whose target support is keyed by each nuisance
coordinate n_i.
Each case matches source count, edge count, per-source frontier size, global
target support, target incidence weights, and uniform per-source entropy.
Declared recovery of d succeeds only for the d-keyed channel.
```

The supported claim would be narrow:

```text
Reachability count is insufficient for declared consequence-bearing recovery.
```

It would not support:

```text
Omega validation;
value or valuer detection;
agency or identity detection;
substrate-general theory validation.
```

Implemented entropy-profile construction:

```text
Use a finite two-bit carrier and two exact channels.
Match per-source support count, per-source entropy, global target support,
global target weights, and global target entropy.
Make one channel recover D_A and fail D_B.
Make the other channel recover D_B and fail D_A.
Emit a retained fixture and a testable pass/fail result.
```

Parameterized family check:

```text
For k = 1..5 nuisance bits, compare a channel preserving declared coordinate
D_0 against channels preserving each nuisance coordinate D_i.
Each case matches source count, edge count, per-source support count,
per-source entropy, global target support, global target weights, and global
target entropy.
Each channel recovers exactly one coordinate distinction, but the recovered
distinction differs.
```

The supported claim is narrow:

```text
Entropy summaries alone do not determine the declared recovery profile.
```

It would not support:

```text
semantic recovery;
identity detection;
Omega validation;
value or valuer detection;
agency detection;
substrate-general theory validation.
```

Implemented mutual-information-vs-declared construction:

```text
Use a finite two-bit source and two deterministic binary-output channels.
Make one channel transmit declared bit d.
Make the other channel transmit nuisance bit n.
Match source count, output support, output weights, output entropy,
conditional output entropy, source-output mutual information, and deterministic
binary-output capacity.
Score declared recovery of d from the binary output.
Emit a retained fixture and a testable pass/fail result.
```

Parameterized family check:

```text
For k = 1..5 nuisance bits, compare transmitting declared d against
transmitting each nuisance coordinate n_i.
Each case matches I(X;Y) = 1 bit and deterministic binary-output capacity = 1
bit under the uniform source distribution.
Declared recovery of d succeeds only for the d-transmitting channel.
```

The supported claim is narrow:

```text
Generic mutual information and deterministic output capacity do not determine
declared registry recovery.
```

It would not support:

```text
semantic recovery;
Omega validation;
value or valuer detection;
agency or identity detection;
substrate-general theory validation.
```

Implemented compression-vs-soundness construction:

```text
Use four fragments and an exact merge profile.
Construct two abstractions with the same class count and class-size signature.
Audit whether same-class pairs are allowed by the exact profile.
Make one abstraction merge only allowed pairs and one abstraction merge blocked
pairs.
Emit a retained fixture and a testable pass/fail result.
```

Parameterized family check:

```text
For k = 1..5 nuisance bits, compare a two-class abstraction keyed by declared
a against two-class abstractions keyed by each nuisance coordinate n_i.
Each case matches fragment count, assignment count, class count, class-size
signature, and simple compression score.
Only the declared-a abstraction is merge-sound under the exact profile where
same declared a allows merge and different declared a blocks merge.
```

The supported claim is narrow:

```text
Compression score alone does not determine merge soundness.
```

It would not support:

```text
optimal compression;
identity detection;
semantic recovery;
Omega validation;
value or valuer detection;
agency detection;
substrate-general abstraction validity.
```

Implemented frontier-morphology-vs-loss construction:

```text
Use a finite two-bit carrier and two one-step support channels.
Match source count, support-edge count, global target support, target weights,
entropy summaries, per-source support counts, and viable-target-count multiset.
Declare a viability predicate over the source carrier.
Measure whether currently viable sources have any viable target in the one-step
support.
Emit a retained fixture and a testable pass/fail result.
```

Parameterized family check:

```text
For k = 1..5 nuisance bits, compare a channel preserving declared viability
bit v against a channel flipping v while leaving nuisance coordinates
unconstrained.
Each case matches source count, edge count, global target support, target
weights, entropy summaries, per-source support counts, and viable-target-count
multiset.
The preserve channel gives zero declared loss over viable sources, while the
flip channel gives loss for every viable source.
```

The supported claim is narrow:

```text
Frontier morphology summaries alone do not determine the declared
horizon-local loss profile.
```

It would not support:

```text
real-world viability;
real irreversibility;
Omega validation;
value or valuer detection;
agency or identity detection;
substrate-general theory validation.
```

Implemented optimized-vs-declared construction:

```text
Use a finite carrier and two finite channels.
Match reachability and entropy controls.
Make both channels exactly recoverable by an optimized observation choice.
Declare one target observation before scoring.
Construct one channel where the declared observation recovers the distinction
and one where only a nondeclared optimized observation recovers it.
Emit a retained fixture and a testable pass/fail result.
```

Parameterized family check:

```text
For k = 1..5 nuisance bits, compare a channel where source d appears in target
coordinate 0 against channels where source d appears in each nondeclared target
coordinate i.
Each case matches source count, edge count, global target support, target
incidence weights, per-source support count, and uniform per-source entropy.
Both channels have exact optimized recovery over the full coordinate panel.
Only the coordinate-0 channel has exact recovery under the declared observation.
```

The supported claim is narrow:

```text
Optimized recovery success is insufficient for declared theorem-transfer
readiness.
```

It would not support:

```text
semantic recovery;
Omega validation;
value or valuer detection;
agency or identity detection;
substrate-general theory validation.
```

Implemented marginal-vs-joint construction:

```text
Use a finite two-bit source carrier and two weighted channels.
Match per-source support count, per-source weight total, per-source entropy,
and Bayes-best recovery success for D_A and D_B.
Construct different Bayes-best recovery success for D_joint.
Emit a retained fixture and a testable pass/fail result.
```

Parameterized family check:

```text
For k = 1..5 nuisance bits, extend the source carrier from AB to ABN...
while scoring only the declared A, B, and AB distinctions.
Each case matches source count, edge count, per-source support count,
per-source weight total, per-source entropy, and Bayes-best recovery success
for D_A and D_B.
The correlated channel keeps D_AB success at 5/8, while the independent-mask
channel keeps D_AB success at 9/16.
```

The supported claim is narrow:

```text
Matched marginal diagnostic success does not determine joint recovery success.
```

It would not support:

```text
exact marginal preservation;
semantic recovery;
Omega validation;
value or valuer detection;
agency or identity detection;
substrate-general theory validation.
```

## Relation To Current Repo

The current routing is:

```text
CLAIMS_LEDGER.md:
  current theorem/result/conjecture split.

REPRODUCIBILITY_SMOKE.md:
  smallest retained empirical reproduction path.

VALIDATION.md:
  local and remote validation paths.

This file:
  baseline map and witness obligations for non-reduction pressure.
```

Future Field Atlas remains a retained morphology instrument. It should stay
demoted unless a specific baseline-controlled witness uses it.
