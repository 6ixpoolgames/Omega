# Frozen Surface Area v0

Status: freeze list / Omega close checkpoint
Scope: branches not pursued further in Omega before successor-spine migration
Claim boundary: process only; not a theorem, not a verdict, not value,
not standing, not agency, not identity, not Omega validation

## Purpose

This note records what is deliberately not being advanced in Omega. The point
is to stop widening the old repository while preserving its evidential record.

## Frozen Until Alpha Pays Prerequisites

```text
patienthood;
cross-valuer compensation;
AuthorityRecord implementation;
valuer-frame transformation law;
quantum rungs;
large-deformer instruments;
Hopf / H2 obstruction hunt;
thermodynamic dictionary implementation;
new joint-tier coordinates;
public manifesto.
```

## Reason

These surfaces require spine-level prerequisites that should live in the
successor repository:

```text
finite lens guard theorem;
intrinsic continuation structure;
generator signatures;
de-finitization ledger;
provenance-preserving migration.
```

## Allowed In Omega

Only closeout work belongs in Omega now:

```text
static certificate staleness;
answerable scope;
retained-result manifest;
migration map;
freeze banner / tag;
validation of already-retained artifacts.
```

## Nonclaims

Freezing a surface does not refute it. It means the old repository will not
promote it without the successor spine.
