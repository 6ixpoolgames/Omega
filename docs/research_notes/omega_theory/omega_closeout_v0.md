# Omega Closeout v0

Status: closeout checkpoint / archive transition note
Scope: terminal state of the Omega research notebook before successor-spine work
Claim boundary: process and navigation only; not new theorem closure, not value,
not standing, not agency, not identity, not patienthood, not Omega validation

## Purpose

This repository is the research notebook and audit record for Omega. It contains
exploratory branches, preregistered protocols, retained witnesses, failed
reductions, demotions, claim-boundary repairs, and theorem scaffolds.

It is not the polished formal spine.

The successor repository should be a monograph-style spine built from retained
results, generator signatures, and provenance links back to this record.

## What Omega Established

Omega established a retained finite and Lean-backed research stack around:

```text
certified presentations and licensing gates;
robust continuation corridors;
switching and adaptive ambiguity distinctions;
sound update and fake-update phantom corridors;
recovery-aware gates and phantom recoverability;
declared loss and expansion profile orders;
order-sampling sensitivity labels;
same-frame CompensationClaim / NOLP v0;
static certificate staleness as a narrow coverage theorem;
reachability-indexed answerable scope;
joint-tier finite instruments and reduction-pressure audits.
```

The current repo also contains exploratory and frozen surfaces that are useful
as provenance but should not be treated as current foundations.

## What Omega Did Not Establish

Omega does not establish:

```text
final value;
standing;
patienthood;
agency;
identity;
cross-valuer compensation;
authority to declare facts or priorities;
large-deformer ethics;
quantum relevance;
Omega validation.
```

It also does not prove the successor repo's planned finite lens guard theorem
or intrinsic continuation structure theorem. Those are Alpha/spine founding
work.

## What Was Retained

Retained artifacts are listed in:

```text
MANIFEST_FREEZE.md
manifest.json
docs/research_notes/omega_theory/retained_results_manifest_v0.md
```

These are the quarry list for successor migration.

## What Was Demoted

Several candidate surfaces were demoted rather than promoted:

```text
joint recovery compatibility:
  recovery-grounded bridge, not an independent axis;

scalar field-measure / population optimum:
  quarantined or deprecated;

future-field language:
  interpretation layer, not foundation;

joint-tier widening:
  frozen after planted-null and reduction audit calibration.
```

## What Was Frozen

Frozen surfaces are listed in:

```text
docs/research_notes/omega_theory/frozen_surface_area_v0.md
```

## Migration Reading

Omega should be read historically:

```text
omega shows the search.
alpha shows the structure.
```

The old history should not be rewritten. Its timestamps, dead ends, audit
packets, retained witnesses, and demotions are part of the evidence chain.

## Public Compression

Omega closes as the auditable lab notebook. The successor repo should refound
the retained results on the minimal spine, with every migrated artifact carrying
a provenance link back to `omega@hash:path`.
