# Joint-Tier Reduction Audit Report v0

Status: retained finite reduction-pressure report
Scope: planted-null controls and scoped reduction attempts over recent joint-tier instruments
Claim boundary: not value, not standing, not agency, not population ethics, not plurality theory, not aggregation, not patienthood, not Omega validation

## Protocol

Protocol note:

```text
docs/research_notes/omega_theory/joint_tier_reduction_audit_protocol_v0.md
```

Validation command:

```powershell
.\.venv\Scripts\python.exe -m omega.validation.finite_relational_joint_tier_reduction_audit --out-root docs\research_notes\validation_results\joint_tier_reduction_audit_v0
```

Retained run:

```text
docs/research_notes/validation_results/joint_tier_reduction_audit_v0/20260707_064631/
```

Retained files:

```text
summary.json
planted_nulls.csv
reduction_attempts.csv
report.md
```

## Verdict

```text
calibrated
```

The planted-null controls reduce as expected, and the scoped reduction attempts
return the conservative verdicts preregistered in the protocol.

This is a reduction-pressure audit. It does not promote a new value claim.

## Planted Null Results

The planted nulls all return:

```text
reduces
```

Nulls:

```text
ensemble span:
  total_l1_amount -> marginal_summary.total_l1_amount

relational composability:
  compatible_pair_count -> CompatibilityProfile.compatible_pair_count

joint recovery compatibility:
  joint_missing_fact_count -> RecoveryProfile.joint_missing_fact_ids

colonization axis:
  viable_state_count -> colonization control_panel.viable_state_count
```

This calibrates the audit harness: known reducible quantities are not treated
as new axes.

## Reduction Attempts

### Relational Composability

Verdict:

```text
survives_simple_graph_scalar_reduction
```

The retained graph-structure robustness witness keeps fixed:

```text
same compatible-pair count;
same degree sequence.
```

while separating:

```text
component structure.
```

This blocks the cheap reductions to edge count or degree sequence. It does not
prove independence from all graph summaries.

### Colonization Axis

Verdict:

```text
survives_scalar_shadow_reduction_lens_debt_open
```

The retained colonization audit keeps the declared control panel fixed and the
scalar-shadow check equal while the certified-chain order still separates.

Open debt:

```text
global lens invariance;
sound-quotient-lattice reduction.
```

This report does not close that debt.

### Joint Recovery Compatibility

Verdict:

```text
bridge_not_independent_axis
```

Joint recovery compatibility should be read as:

```text
registered coupling surface + declared recovery labels
```

rather than as a fresh independent coordinate. That is useful for NOLP because
it ties compatibility to recovery behavior, but it should not be promoted as a
new axis.

## What This Changes

The near-term queue is now cleaner:

```text
1. use joint recovery compatibility as a bridge input;
2. do not widen the joint-tier instrument chain immediately;
3. start CompensationClaim / NOLP only after retaining this audit output;
4. reserve the lens theorem for a dedicated proof sprint.
```

## Nonclaims

This report does not claim:

```text
value;
standing;
agency;
population ethics;
plurality theory;
aggregation;
patienthood;
Omega validation.
```

## Public Compression

The joint-tier reduction audit calibrates the instrument family before NOLP:
known reducible coordinates reduce, relational composability survives cheap
graph-scalar reductions, colonization keeps a visible lens-theorem debt, and
joint recovery compatibility is treated as a recovery-grounded bridge rather
than an independent axis.
