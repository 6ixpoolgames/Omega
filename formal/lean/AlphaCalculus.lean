import ProtoOmega

/-!
AlphaCalculus public entry point.

This is the current public name for the derived formal machinery over
`AlphaCore`: presentations, transport, recoverability, recurrence, and finite
separation results. The older `ProtoOmega` module remains as a compatibility
implementation namespace during migration.
-/
