import OmegaProper.Trajectory.ConsequenceClasses

/-!
OmegaProper.Trajectory.ClassSoundnessAsClique

Standard graph-language compression for class soundness.

Sound classes are cliques in the compatibility graph. Chain-connectedness is
not enough: the three-vertex path is the minimal counterexample.
-/

namespace OmegaProper
namespace Trajectory
namespace ClassSoundnessAsClique

open ConsequenceRelation
open ConsequenceClasses

universe u w k o

/-- A reflexive symmetric relation. -/
def IsTolerance {α : Type u} (r : α -> α -> Prop) : Prop :=
  (forall x, r x x) /\ (forall x y, r x y -> r y x)

/-- A class is a clique when every pair of its members is related. -/
def IsClique {α : Type u} (r : α -> α -> Prop) (member : α -> Prop) : Prop :=
  forall x y, member x -> member y -> r x y

/-- Path/chain evidence generated by a binary relation. -/
inductive Chain {α : Type u} (r : α -> α -> Prop) : α -> α -> Prop where
  | base {x y} : r x y -> Chain r x y
  | step {x y z} : Chain r x y -> r y z -> Chain r x z

/--
The existing consequence class guardrail is exactly clique soundness for the
directional compatibility graph.
-/
theorem classRespectsConsequences_iff_clique
    (S : ConsequenceSystem.{w, k, o})
    (member : S.Fragment -> Prop) :
    ClassRespectsConsequences S member <->
      IsClique (ConsequenceCompatible S) member := by
  rfl

/-! ## Minimal path counterexample -/

inductive Path3 where
  | a
  | b
  | c
  deriving DecidableEq

/-- Loops plus adjacent undirected edges on the three-vertex path. -/
def path3Edge : Path3 -> Path3 -> Prop
  | Path3.a, Path3.a => True
  | Path3.a, Path3.b => True
  | Path3.b, Path3.a => True
  | Path3.b, Path3.b => True
  | Path3.b, Path3.c => True
  | Path3.c, Path3.b => True
  | Path3.c, Path3.c => True
  | _, _ => False

def path3FullClass (_x : Path3) : Prop := True

theorem path3_tolerance : IsTolerance path3Edge := by
  constructor
  · intro x
    cases x <;> trivial
  · intro x y h
    cases x <;> cases y <;> try trivial <;> cases h

theorem path3_chain_ac : Chain path3Edge Path3.a Path3.c := by
  exact Chain.step
    (x := Path3.a)
    (y := Path3.b)
    (z := Path3.c)
    (Chain.base (by trivial))
    (by trivial)

theorem path3_not_ac : Not (path3Edge Path3.a Path3.c) := by
  intro h
  cases h

theorem path3_full_not_clique : Not (IsClique path3Edge path3FullClass) := by
  intro hClique
  exact path3_not_ac (hClique Path3.a Path3.c trivial trivial)

/--
There is a tolerance relation with chain-connected endpoints that are not
compatible; hence chain evidence does not license class soundness.
-/
theorem chain_evidence_is_not_class_soundness :
    IsTolerance path3Edge /\
      Chain path3Edge Path3.a Path3.c /\
      Not (path3Edge Path3.a Path3.c) /\
      Not (IsClique path3Edge path3FullClass) := by
  exact And.intro path3_tolerance
    (And.intro path3_chain_ac
      (And.intro path3_not_ac path3_full_not_clique))

end ClassSoundnessAsClique
end Trajectory
end OmegaProper
