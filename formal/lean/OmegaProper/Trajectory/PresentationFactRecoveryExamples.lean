import OmegaProper.Recovery.Examples
import OmegaProper.Trajectory.PresentationFactClosure

/-!
OmegaProper.Trajectory.PresentationFactRecoveryExamples

Finite recovery-fact examples for presentation/fact closure.

This module instantiates the closure layer with an actual recovery predicate:
a presentation is an observation map, and a claim is a declared target. The
satisfaction relation says that the target is exactly recoverable at threshold
one through that observation on the identity two-state channel.

The example shows the same adversarial shape as the coordinate pilot, but now
for recovery:

* the declared bit target is common to the exact-observation family;
* adding a constant observation removes that recovery fact;
* constant targets still survive the larger family;
* the constant observation is outside the closure generated by exact recovery
  facts.

This does not define admissibility or empirical observation validity.
-/

namespace OmegaProper
namespace Trajectory
namespace PresentationFactRecoveryExamples

open PresentationFactClosure
open Recovery
open Recovery.Examples

/-- Constant Bit-valued observation. It erases the two output labels. -/
def constantZeroBitObserve (_y : Bit) : Bit := 0

/-- Constant Bit-valued target, used as a nontriviality control. -/
def constantZeroBitTarget (_x : Bit) : Bit := 0

/-- Declared presentation family containing only the exact bit observation. -/
def exactOnlyRecoveryPresentations : Set (Bit -> Bit) :=
  fun observe => observe = bitObserve

/-- Expanded family containing the exact observation and a constant observation. -/
def exactConstantRecoveryPresentations : Set (Bit -> Bit) :=
  fun observe => observe = bitObserve ∨ observe = constantZeroBitObserve

/--
Recovery satisfaction: an observation satisfies a target when the identity
two-state channel recovers that target exactly through the observation.
-/
def BitRecoverySatisfies (observe : Bit -> Bit) (target : Bit -> Bit) : Prop :=
  RecoveryExistsAt identityBitChannel target observe 1

/-- Common exactly recoverable targets for a declared observation family. -/
abbrev CommonRecoveryTargets (presentations : Set (Bit -> Bit)) :
    Set (Bit -> Bit) :=
  CommonClaims BitRecoverySatisfies presentations

theorem exactOnly_subset_exactConstant :
    exactOnlyRecoveryPresentations ⊆ exactConstantRecoveryPresentations := by
  intro observe hObserve
  exact Or.inl hObserve

theorem bitTarget_common_exactOnly :
    bitTarget ∈ CommonRecoveryTargets exactOnlyRecoveryPresentations := by
  intro observe hObserve
  rw [hObserve]
  exact identityBitChannel_recoveryAt_one

theorem constantZeroBitObserve_not_recoveryAt_one :
    Not (
      RecoveryExistsAt
        identityBitChannel
        bitTarget
        constantZeroBitObserve
        1
    ) := by
  intro hRecovery
  match hRecovery with
  | Exists.intro decoder hDecoder =>
      rcases bit_eq_zero_or_one (decoder 0) with hDecoderZero | hDecoderZero
      · have hSrc := hDecoder 1
        norm_num [DeclaredRecoveryAt, Success, identityBitChannel, bitTarget,
          constantZeroBitObserve, hDecoderZero, Finset.univ_fin2] at hSrc
      · have hSrc := hDecoder 0
        norm_num [DeclaredRecoveryAt, Success, identityBitChannel, bitTarget,
          constantZeroBitObserve, hDecoderZero, Finset.univ_fin2] at hSrc

theorem bitTarget_not_common_exactConstant :
    Not (bitTarget ∈ CommonRecoveryTargets exactConstantRecoveryPresentations) := by
  intro hCommon
  have hRecovery :=
    hCommon constantZeroBitObserve (Or.inr rfl)
  exact constantZeroBitObserve_not_recoveryAt_one hRecovery

theorem constantZeroBitTarget_recoveryAt_one
    (observe : Bit -> Bit) :
    RecoveryExistsAt
      identityBitChannel
      constantZeroBitTarget
      observe
      1 := by
  exact Exists.intro (fun _ => 0) fun x => by
    fin_cases x <;>
      norm_num [DeclaredRecoveryAt, Success, identityBitChannel,
        constantZeroBitTarget, Finset.univ_fin2]

theorem constantZeroBitTarget_common_exactConstant :
    constantZeroBitTarget ∈
      CommonRecoveryTargets exactConstantRecoveryPresentations := by
  intro observe _hObserve
  exact constantZeroBitTarget_recoveryAt_one observe

/--
Adding a constant observation strictly shrinks the common recovery facts:
everything common to the larger family is common to the exact-only family, but
the declared bit target itself is lost.
-/
theorem commonRecoveryTargets_strictly_shrink_when_constant_admitted :
    CommonRecoveryTargets exactConstantRecoveryPresentations ⊆
        CommonRecoveryTargets exactOnlyRecoveryPresentations ∧
      bitTarget ∈ CommonRecoveryTargets exactOnlyRecoveryPresentations ∧
      Not (bitTarget ∈ CommonRecoveryTargets exactConstantRecoveryPresentations) := by
  exact And.intro
    (commonClaims_antitone exactOnly_subset_exactConstant)
    (And.intro
      bitTarget_common_exactOnly
      bitTarget_not_common_exactConstant)

/-- The exact observation is included in the closure generated by exact facts. -/
theorem bitObserve_in_recoveryClosure_exactOnly :
    bitObserve ∈
      PresentationClosure BitRecoverySatisfies exactOnlyRecoveryPresentations := by
  exact presentations_subset_closure rfl

/--
The constant observation is excluded from the recovery closure generated by the
exact-observation family, because it fails the bit target that the exact family
recovers.
-/
theorem constantObserve_not_in_recoveryClosure_exactOnly :
    Not (constantZeroBitObserve ∈
      PresentationClosure BitRecoverySatisfies exactOnlyRecoveryPresentations) := by
  intro hClosure
  exact constantZeroBitObserve_not_recoveryAt_one
    (hClosure bitTarget bitTarget_common_exactOnly)

end PresentationFactRecoveryExamples
end Trajectory
end OmegaProper
