import OmegaProper.Trajectory.CarrierCertificate
import OmegaProper.Trajectory.PresentationFactClosure

/-!
OmegaProper.Trajectory.PresentationFactCarrierExamples

Finite carrier-certificate examples for presentation/fact closure.

This module instantiates the closure layer with the existing two-state
recurrent carrier certificate. The carrier certificate itself is checked by
the recurrent-support stack; this file asks a narrower presentation question:

* an endpoint-status presentation keeps the certified pair visible;
* admitting a constant presentation removes every visible-pair fact from the
  common facts;
* the constant presentation is excluded from the closure generated by the
  exact endpoint-visibility facts.

This is not an identity, objecthood, boundary, agency, value, or Omega claim.
It only records how certified carrier visibility behaves under a declared
presentation family.
-/

namespace OmegaProper
namespace Trajectory
namespace PresentationFactCarrierExamples

open CarrierCertificate
open CarriedDistinction
open PresentationFactClosure
open PresentationInvariant
open SustainingViableClass

/-- Exact Boolean endpoint status for the two-state carrier witness. -/
def carrierEndpointStatus : CycleState -> Bool
  | .left => true
  | .right => false

/-- Constant carrier-status presentation that erases the endpoint contrast. -/
def constantFalseCarrierStatus (_x : CycleState) : Bool := false

/-- The certified carrier endpoint pair from the two-state cycle witness. -/
def cycleCarrierPair : CycleState × CycleState :=
  (CycleState.left, CycleState.right)

/-- Declared presentation family containing only exact endpoint status. -/
def carrierExactOnlyPresentations : Set (CycleState -> Bool) :=
  fun present => present = carrierEndpointStatus

/-- Expanded family containing exact endpoint status and a constant view. -/
def carrierExactConstantPresentations : Set (CycleState -> Bool) :=
  fun present =>
    present = carrierEndpointStatus ∨
      present = constantFalseCarrierStatus

/-- Carrier-pair satisfaction is ordinary pair visibility under a presentation. -/
abbrev CarrierPairVisibleSatisfies :
    (CycleState -> Bool) -> (CycleState × CycleState) -> Prop :=
  PairVisibleSatisfiesPresentation

/-- Common carrier-visible pairs for a declared presentation family. -/
abbrev CommonCarrierVisiblePairs
    (presentations : Set (CycleState -> Bool)) :
    Set (CycleState × CycleState) :=
  CommonClaims CarrierPairVisibleSatisfies presentations

/-- The two-state cycle already certifies the declared carrier pair. -/
theorem cycleCarrierPair_certified :
    CarrierCertificate
      cycleConsequenceSystem
      cycleNext
      cycleSafe
      cycleClass
      CycleState.left
      CycleState.right := by
  exact cycle_carrier_certificate

/-- Exact endpoint status does not erase the certified endpoint pair. -/
theorem carrierEndpointStatus_keeps_cyclePair_visible :
    CarrierPairVisibleSatisfies carrierEndpointStatus cycleCarrierPair := by
  intro hErased
  cases hErased

theorem carrierExactOnly_subset_carrierExactConstant :
    carrierExactOnlyPresentations ⊆ carrierExactConstantPresentations := by
  intro present hPresent
  exact Or.inl hPresent

/-- The certified pair is visible in the exact-endpoint presentation family. -/
theorem cycleCarrierPair_visible_exactOnly :
    cycleCarrierPair ∈
      CommonCarrierVisiblePairs carrierExactOnlyPresentations := by
  intro present hPresent
  change present = carrierEndpointStatus at hPresent
  rw [hPresent]
  exact carrierEndpointStatus_keeps_cyclePair_visible

/--
Admitting the constant carrier-status presentation removes all visible-pair
facts from the common visible-pair set.
-/
theorem no_commonCarrierVisiblePairs_exactConstant
    (pair : CycleState × CycleState) :
    Not (
      pair ∈
        CommonCarrierVisiblePairs carrierExactConstantPresentations
    ) := by
  intro hCommon
  have hVisible :=
    hCommon constantFalseCarrierStatus (by
      exact Or.inr rfl)
  exact hVisible rfl

/-- The certified pair is not visible in the expanded presentation family. -/
theorem cycleCarrierPair_not_visible_exactConstant :
    Not (
      cycleCarrierPair ∈
        CommonCarrierVisiblePairs carrierExactConstantPresentations
    ) := by
  exact no_commonCarrierVisiblePairs_exactConstant cycleCarrierPair

/--
Adding a constant carrier-status presentation strictly shrinks common
carrier-visible facts: everything common to the larger family is common to the
exact-only family, but the certified pair itself is lost.
-/
theorem commonCarrierVisiblePairs_strictly_shrink_when_constant_admitted :
    CommonCarrierVisiblePairs carrierExactConstantPresentations ⊆
        CommonCarrierVisiblePairs carrierExactOnlyPresentations ∧
      cycleCarrierPair ∈
        CommonCarrierVisiblePairs carrierExactOnlyPresentations ∧
      Not (
        cycleCarrierPair ∈
          CommonCarrierVisiblePairs carrierExactConstantPresentations
      ) := by
  exact And.intro
    (commonClaims_antitone carrierExactOnly_subset_carrierExactConstant)
    (And.intro
      cycleCarrierPair_visible_exactOnly
      cycleCarrierPair_not_visible_exactConstant)

/-- The exact endpoint presentation is included in its induced closure. -/
theorem carrierEndpointStatus_in_carrierVisibilityClosure_exactOnly :
    carrierEndpointStatus ∈
      PresentationClosure
        CarrierPairVisibleSatisfies
        carrierExactOnlyPresentations := by
  exact presentations_subset_closure rfl

/--
The constant carrier-status presentation is excluded from the closure generated
by exact endpoint-visibility facts.
-/
theorem constantStatus_not_in_carrierVisibilityClosure_exactOnly :
    Not (
      constantFalseCarrierStatus ∈
        PresentationClosure
          CarrierPairVisibleSatisfies
          carrierExactOnlyPresentations
    ) := by
  intro hClosure
  have hVisible :
      CarrierPairVisibleSatisfies
        constantFalseCarrierStatus
        cycleCarrierPair :=
    hClosure cycleCarrierPair cycleCarrierPair_visible_exactOnly
  exact hVisible rfl

/--
Compact bridge statement: the pair has a recurrent carrier certificate and is
visible under the exact endpoint-presentation family, but it is not visible
after the erasing constant presentation is admitted.
-/
theorem certified_cycleCarrierPair_visibility_boundary :
    CarrierCertificate
        cycleConsequenceSystem
        cycleNext
        cycleSafe
        cycleClass
        CycleState.left
        CycleState.right ∧
      cycleCarrierPair ∈
        CommonCarrierVisiblePairs carrierExactOnlyPresentations ∧
      Not (
        cycleCarrierPair ∈
          CommonCarrierVisiblePairs carrierExactConstantPresentations
      ) := by
  exact And.intro
    cycleCarrierPair_certified
    (And.intro
      cycleCarrierPair_visible_exactOnly
      cycleCarrierPair_not_visible_exactConstant)

end PresentationFactCarrierExamples
end Trajectory
end OmegaProper
