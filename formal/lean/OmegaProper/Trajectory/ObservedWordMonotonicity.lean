import Mathlib.Data.Finset.Card
import OmegaProper.Trajectory.ViabilityReflection
import OmegaProper.Trajectory.ViableTrajectoryLanguage

/-!
OmegaProper.Trajectory.ObservedWordMonotonicity

Observed finite-word monotonicity under process-coherent presentation.

The finite relational adapter now distinguishes global edge-image exactness
from representative-wise step/path lifting. This file extracts the settled
formal core:

* observed extendable safe words are generated by safe finite paths whose
  endpoint is viable;
* representative-wise step lifting, safety reflection, and observation
  commutation lift every abstract observed extendable safe word to an exact
  observed extendable safe word;
* language inclusion implies finite count monotonicity for any retained
  `Finset` of words;
* global edge-image exactness is not enough to obtain step lifting.

This is a process-coherence theorem. It does not define value, lushness,
agency, identity, alignment, or Omega proper.
-/

namespace OmegaProper
namespace Trajectory
namespace ObservedWordMonotonicity

open ReachabilityReflection
open ReachabilityViability
open TrajectorySemantics
open ViabilityReflection

universe u v w

/--
An observed safe path of transition length `n`.

The word records observations of states along the path, including the start and
endpoint. Thus a zero-length path has a singleton word.
-/
inductive ObservedSafePath
    (D : Dyn.{u})
    (safe : D.State -> Prop)
    (observe : D.State -> W) :
    Nat -> D.State -> List W -> D.State -> Prop where
  | zero {x : D.State} :
      safe x ->
      ObservedSafePath D safe observe 0 x [observe x] x
  | step {n : Nat} {x y z : D.State} {tail : List W} :
      safe x ->
      D.Next x y ->
      ObservedSafePath D safe observe n y tail z ->
      ObservedSafePath D safe observe (Nat.succ n) x (observe x :: tail) z

/--
An observed extendable safe word is an observed safe path whose endpoint lies
in the viability kernel. This is the formal counterpart of the adapter's
`observed_extendable_safe_word_count` surface, minus any counting.
-/
def ObservedExtendableSafeWord
    (D : Dyn.{u})
    (safe : D.State -> Prop)
    (observe : D.State -> W)
    (n : Nat)
    (x : D.State)
    (word : List W) : Prop :=
  exists y : D.State,
    ObservedSafePath D safe observe n x word y /\ Viable D safe y

/-- The finite observed extendable safe language at a start state and horizon. -/
def ObservedExtendableSafeLanguageAt
    (D : Dyn.{u})
    (safe : D.State -> Prop)
    (observe : D.State -> W)
    (n : Nat)
    (x : D.State) : List W -> Prop :=
  fun word => ObservedExtendableSafeWord D safe observe n x word

/-- Observation labels commute with a presentation. -/
def ObservationCommutes
    (DX : Dyn.{u})
    (DQ : Dyn.{v})
    (present : DX.State -> DQ.State)
    (observeX : DX.State -> W)
    (observeQ : DQ.State -> W) : Prop :=
  forall x, observeX x = observeQ (present x)

/--
Minimal semantic condition: every abstract observed extendable safe word from a
presented exact state has an exact realization with the same observed word.
-/
def ObservedViablePathLift
    (DX : Dyn.{u})
    (DQ : Dyn.{v})
    (present : DX.State -> DQ.State)
    (safeX : DX.State -> Prop)
    (safeQ : DQ.State -> Prop)
    (observeX : DX.State -> W)
    (observeQ : DQ.State -> W) : Prop :=
  forall n x word,
    ObservedExtendableSafeWord DQ safeQ observeQ n (present x) word ->
      ObservedExtendableSafeWord DX safeX observeX n x word

/--
Audit-friendly sufficient contract. It is stronger than the minimal semantic
condition, but easy to check locally: abstract safety reflects to exact safety,
abstract steps lift representative-wise, and observations commute.
-/
structure StrongObservedWordTransport
    (DX : Dyn.{u})
    (DQ : Dyn.{v})
    (present : DX.State -> DQ.State)
    (safeX : DX.State -> Prop)
    (safeQ : DQ.State -> Prop)
    (observeX : DX.State -> W)
    (observeQ : DQ.State -> W) where
  safe_reflects : SafeReflects DX DQ present safeX safeQ
  step_reflects : StepReflects DX DQ present
  observations_commute : ObservationCommutes DX DQ present observeX observeQ

/-- Lift an observed safe path along a representative-wise step-lifting map. -/
theorem observedSafePath_lifts_exact
    {DX : Dyn.{u}}
    {DQ : Dyn.{v}}
    {present : DX.State -> DQ.State}
    {safeX : DX.State -> Prop}
    {safeQ : DQ.State -> Prop}
    {observeX : DX.State -> W}
    {observeQ : DQ.State -> W}
    (hSafe : SafeReflects DX DQ present safeX safeQ)
    (hStep : StepReflects DX DQ present)
    (hObs : ObservationCommutes DX DQ present observeX observeQ)
    {n : Nat}
    {x : DX.State}
    {q : DQ.State}
    {word : List W}
    {qEnd : DQ.State}
    (hStart : present x = q)
    (hPathQ : ObservedSafePath DQ safeQ observeQ n q word qEnd) :
    exists yEnd : DX.State,
      ObservedSafePath DX safeX observeX n x word yEnd /\
        present yEnd = qEnd := by
  induction hPathQ generalizing x with
  | zero hSafeQ =>
      exact Exists.intro x
        (And.intro
          (by
            simpa [hStart, hObs x] using
              (ObservedSafePath.zero
                (D := DX)
                (safe := safeX)
                (observe := observeX)
                (x := x)
                  (hSafe x (by
                    simpa [hStart] using hSafeQ))))
          hStart)
  | step hSafeQ hStepQ _hRest ih =>
      rename_i nTail qStart qNext qEnd' tail
      match hStep x qNext (by
          simpa [hStart] using hStepQ) with
      | Exists.intro y hy =>
          match ih hy.right with
          | Exists.intro z hz =>
              exact Exists.intro z
                (And.intro
                  (by
                    simpa [hStart, hObs x] using
                      (ObservedSafePath.step
                        (D := DX)
                        (safe := safeX)
                        (observe := observeX)
                        (hSafe x (by
                          simpa [hStart] using hSafeQ))
                        hy.left
                        hz.left))
                  hz.right)

/--
Strong observed-word transport implies the minimal observed viable path-lifting
condition.
-/
theorem strongObservedWordTransport_observedViablePathLift
    {DX : Dyn.{u}}
    {DQ : Dyn.{v}}
    {present : DX.State -> DQ.State}
    {safeX : DX.State -> Prop}
    {safeQ : DQ.State -> Prop}
    {observeX : DX.State -> W}
    {observeQ : DQ.State -> W}
    (hTransport :
      StrongObservedWordTransport
        DX DQ present safeX safeQ observeX observeQ) :
    ObservedViablePathLift
      DX DQ present safeX safeQ observeX observeQ := by
  intro n x word hWordQ
  match hWordQ with
  | Exists.intro qEnd hq =>
      match observedSafePath_lifts_exact
          hTransport.safe_reflects
          hTransport.step_reflects
          hTransport.observations_commute
          rfl
          hq.left with
      | Exists.intro yEnd hy =>
          have hViableX : Viable DX safeX yEnd := by
            exact abstractViable_reflects_exactViable_of_reflects
              hTransport.safe_reflects
              hTransport.step_reflects
              (x := yEnd)
              (by
                simpa [hy.right] using hq.right)
          exact Exists.intro yEnd
            (And.intro hy.left hViableX)

/-- The minimal lifting condition gives observed-language inclusion. -/
theorem observedLanguage_subset_of_observedViablePathLift
    {DX : Dyn.{u}}
    {DQ : Dyn.{v}}
    {present : DX.State -> DQ.State}
    {safeX : DX.State -> Prop}
    {safeQ : DQ.State -> Prop}
    {observeX : DX.State -> W}
    {observeQ : DQ.State -> W}
    (hLift :
      ObservedViablePathLift
        DX DQ present safeX safeQ observeX observeQ)
    (n : Nat)
    (x : DX.State) :
    forall word,
      ObservedExtendableSafeLanguageAt DQ safeQ observeQ n (present x) word ->
        ObservedExtendableSafeLanguageAt DX safeX observeX n x word := by
  intro word hWord
  exact hLift n x word hWord

/--
Finite count corollary: once an implementation has retained finite word sets,
language inclusion implies cardinal monotonicity.
-/
theorem finiteObservedWordCount_mono_of_subset
    {W : Type w}
    [DecidableEq W]
    {abstractWords exactWords : Finset W}
    (hSubset : forall word, word ∈ abstractWords -> word ∈ exactWords) :
    abstractWords.card <= exactWords.card := by
  exact Finset.card_le_card hSubset

/-- Global projected edge equality, intentionally weaker than step lifting. -/
def EdgeImageExact
    (DX : Dyn.{u})
    (DQ : Dyn.{v})
    (present : DX.State -> DQ.State) : Prop :=
  forall q q',
    DQ.Next q q' <->
      exists x y,
        present x = q /\ present y = q' /\ DX.Next x y

/--
Small splice system for the negative control.

The exact system has edges `a -> b` and `c -> d`. The abstract presentation
merges `b` and `c` into `m`, yielding abstract edges `A -> m` and `m -> D`.
Every abstract edge is globally witnessed by some exact edge, but the abstract
history `A -> m -> D` switches hidden representatives and cannot be lifted from
exact `a`.
-/
inductive SpliceExact where
  | a | b | c | d

inductive SpliceAbstract where
  | A | m | D

def splicePresent : SpliceExact -> SpliceAbstract
  | SpliceExact.a => SpliceAbstract.A
  | SpliceExact.b => SpliceAbstract.m
  | SpliceExact.c => SpliceAbstract.m
  | SpliceExact.d => SpliceAbstract.D

def spliceExactNext : SpliceExact -> SpliceExact -> Prop
  | SpliceExact.a, SpliceExact.b => True
  | SpliceExact.c, SpliceExact.d => True
  | _, _ => False

def spliceAbstractNext : SpliceAbstract -> SpliceAbstract -> Prop
  | SpliceAbstract.A, SpliceAbstract.m => True
  | SpliceAbstract.m, SpliceAbstract.D => True
  | _, _ => False

def spliceExactDyn : Dyn where
  State := SpliceExact
  Next := spliceExactNext

def spliceAbstractDyn : Dyn where
  State := SpliceAbstract
  Next := spliceAbstractNext

theorem splice_edgeImageExact :
    EdgeImageExact spliceExactDyn spliceAbstractDyn splicePresent := by
  intro q q'
  constructor
  · intro hAbs
    cases q <;> cases q' <;> try cases hAbs
    · exact Exists.intro SpliceExact.a
        (Exists.intro SpliceExact.b
          (And.intro rfl (And.intro rfl trivial)))
    · exact Exists.intro SpliceExact.c
        (Exists.intro SpliceExact.d
          (And.intro rfl (And.intro rfl trivial)))
  · intro hProjected
    rcases hProjected with ⟨x, y, hxq, hyq, hEdge⟩
    cases x <;> cases y <;>
      simp [spliceExactDyn, spliceAbstractDyn, splicePresent,
        spliceExactNext, spliceAbstractNext] at hxq hyq hEdge ⊢
    all_goals
      subst q
      subst q'
      trivial

theorem splice_not_stepReflects :
    Not (StepReflects spliceExactDyn spliceAbstractDyn splicePresent) := by
  intro hStep
  have hAbstractStep :
      spliceAbstractDyn.Next
        (splicePresent SpliceExact.b)
        SpliceAbstract.D := by
    trivial
  match hStep SpliceExact.b SpliceAbstract.D hAbstractStep with
  | Exists.intro y hy =>
      cases y <;> cases hy.left

/-- A concrete two-step path predicate for the splice negative control. -/
def TwoStep {A : Type u} (Next : A -> A -> Prop) (x z : A) : Prop :=
  exists y, Next x y /\ Next y z

theorem splice_abstractTwoStep_a_to_D :
    TwoStep spliceAbstractNext SpliceAbstract.A SpliceAbstract.D := by
  exact Exists.intro SpliceAbstract.m
    (And.intro trivial trivial)

theorem splice_no_exactTwoStep_a_to_D_fiber :
    Not (exists y : SpliceExact,
      splicePresent y = SpliceAbstract.D /\
        TwoStep spliceExactNext SpliceExact.a y) := by
  intro hPath
  rcases hPath with ⟨y, hPresent, hTwo⟩
  rcases hTwo with ⟨mid, hFirst, hSecond⟩
  cases mid <;> cases y <;>
    simp [splicePresent, spliceExactNext] at hPresent hFirst hSecond

theorem edgeImageExact_does_not_imply_stepReflects :
    EdgeImageExact spliceExactDyn spliceAbstractDyn splicePresent /\
      Not (StepReflects spliceExactDyn spliceAbstractDyn splicePresent) := by
  exact And.intro splice_edgeImageExact splice_not_stepReflects

theorem edgeImageExact_does_not_imply_pathLifting :
    EdgeImageExact spliceExactDyn spliceAbstractDyn splicePresent /\
      TwoStep spliceAbstractNext
        (splicePresent SpliceExact.a)
        SpliceAbstract.D /\
      Not (exists y : SpliceExact,
        splicePresent y = SpliceAbstract.D /\
          TwoStep spliceExactNext SpliceExact.a y) := by
  exact And.intro splice_edgeImageExact
    (And.intro
      splice_abstractTwoStep_a_to_D
      splice_no_exactTwoStep_a_to_D_fiber)

end ObservedWordMonotonicity
end Trajectory
end OmegaProper
