import OmegaProper.Trajectory.IrreversibleViabilityLoss
import OmegaProper.Trajectory.PresentationFactClosure

/-!
OmegaProper.Trajectory.PresentationFactViabilityExamples

Finite viability-status examples for presentation/fact closure.

This module instantiates the closure layer with the tiny irreversible viability
loss witness. The exact dynamics already prove:

* `loop` is viable;
* `dead` is not viable;
* `loop -> dead` is a viability-loss step.

We expose that exact viability contrast as a finite Boolean status target and
show the closure behavior:

* the viability-status target is common to the exact-status presentation;
* adding a constant presentation removes that fact;
* constant targets still survive the expanded family;
* the constant presentation is outside the closure generated by exact
  viability-status facts.

This is a presentation/fact closure pilot for viability facts, not a general
admissibility criterion.
-/

namespace OmegaProper
namespace Trajectory
namespace PresentationFactViabilityExamples

open IrreversibleViabilityLoss
open PresentationFactClosure
open ReachabilityViability
open TargetPresentationInvariant

/-- Exact Boolean status corresponding to the tiny viability witness. -/
def viabilityStatus : ViabilityLossState -> Bool
  | ViabilityLossState.loop => true
  | ViabilityLossState.dead => false

/-- A constant status presentation that erases the viability contrast. -/
def constantFalseStatus (_x : ViabilityLossState) : Bool := false

/-- Constant target, used as a nontriviality control. -/
def constantFalseTarget (_x : ViabilityLossState) : Bool := false

/-- Declared presentation family containing only exact viability status. -/
def viabilityExactOnlyPresentations : Set (ViabilityLossState -> Bool) :=
  fun present => present = viabilityStatus

/-- Expanded family containing exact status and the constant status view. -/
def viabilityExactConstantPresentations : Set (ViabilityLossState -> Bool) :=
  fun present => present = viabilityStatus ∨ present = constantFalseStatus

/-- Viability-status satisfaction is ordinary target preservation on fibers. -/
abbrev ViabilityStatusSatisfies :
    (ViabilityLossState -> Bool) -> (ViabilityLossState -> Bool) -> Prop :=
  TargetSatisfiesPresentation

/-- Common viability-status facts for a declared presentation family. -/
abbrev CommonViabilityStatusFacts
    (presentations : Set (ViabilityLossState -> Bool)) :
    Set (ViabilityLossState -> Bool) :=
  CommonClaims ViabilityStatusSatisfies presentations

theorem viabilityStatus_records_exact_witness :
    viabilityStatus ViabilityLossState.loop = true ∧
      viabilityStatus ViabilityLossState.dead = false ∧
      Viable viabilityLossDyn viabilityLossSafe ViabilityLossState.loop ∧
      Not (Viable viabilityLossDyn viabilityLossSafe ViabilityLossState.dead) := by
  exact And.intro rfl
    (And.intro rfl
      (And.intro loop_viable dead_not_viable))

theorem viabilityStatus_separates_loop_dead :
    TargetSeparatedBy
      viabilityStatus
      ViabilityLossState.loop
      ViabilityLossState.dead := by
  intro hEq
  cases hEq

theorem viabilityExactOnly_subset_viabilityExactConstant :
    viabilityExactOnlyPresentations ⊆ viabilityExactConstantPresentations := by
  intro present hPresent
  exact Or.inl hPresent

theorem viabilityStatus_common_exactOnly :
    viabilityStatus ∈ CommonViabilityStatusFacts viabilityExactOnlyPresentations := by
  intro present hPresent x y hErased
  rw [hPresent] at hErased
  exact hErased

theorem viabilityStatus_not_common_exactConstant :
    Not (
      viabilityStatus ∈
        CommonViabilityStatusFacts viabilityExactConstantPresentations
    ) := by
  intro hCommon
  have hRespect := hCommon constantFalseStatus (Or.inr rfl)
  have hEq :
      viabilityStatus ViabilityLossState.loop =
        viabilityStatus ViabilityLossState.dead :=
    hRespect ViabilityLossState.loop ViabilityLossState.dead rfl
  exact viabilityStatus_separates_loop_dead hEq

theorem constantFalseTarget_common_exactConstant :
    constantFalseTarget ∈
      CommonViabilityStatusFacts viabilityExactConstantPresentations := by
  intro _present _hPresent _x _y _hErased
  rfl

/--
Adding the constant status presentation strictly shrinks common viability-status
facts: all facts common to the larger family are common to the exact-only
family, but the exact viability status itself is lost.
-/
theorem commonViabilityFacts_strictly_shrink_when_constant_admitted :
    CommonViabilityStatusFacts viabilityExactConstantPresentations ⊆
        CommonViabilityStatusFacts viabilityExactOnlyPresentations ∧
      viabilityStatus ∈
        CommonViabilityStatusFacts viabilityExactOnlyPresentations ∧
      Not (
        viabilityStatus ∈
          CommonViabilityStatusFacts viabilityExactConstantPresentations
      ) := by
  exact And.intro
    (commonClaims_antitone viabilityExactOnly_subset_viabilityExactConstant)
    (And.intro
      viabilityStatus_common_exactOnly
      viabilityStatus_not_common_exactConstant)

/-- The exact status presentation is included in its induced closure. -/
theorem viabilityStatus_in_viabilityClosure_exactOnly :
    viabilityStatus ∈
      PresentationClosure
        ViabilityStatusSatisfies
        viabilityExactOnlyPresentations := by
  exact presentations_subset_closure rfl

/--
The constant status presentation is excluded from the closure generated by
exact viability-status facts.
-/
theorem constantStatus_not_in_viabilityClosure_exactOnly :
    Not (
      constantFalseStatus ∈
        PresentationClosure
          ViabilityStatusSatisfies
          viabilityExactOnlyPresentations
    ) := by
  intro hClosure
  have hRespect : TargetSatisfiesPresentation constantFalseStatus viabilityStatus :=
    hClosure viabilityStatus viabilityStatus_common_exactOnly
  have hEq :
      viabilityStatus ViabilityLossState.loop =
        viabilityStatus ViabilityLossState.dead :=
    hRespect ViabilityLossState.loop ViabilityLossState.dead rfl
  exact viabilityStatus_separates_loop_dead hEq

end PresentationFactViabilityExamples
end Trajectory
end OmegaProper
