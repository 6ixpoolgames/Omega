import OmegaCore.DistTrans

/-!
ProtoOmega.Transport.Preorder

Derived distinction-transport presentation over the Alpha primitive floor.
This facade currently re-exports the checked legacy `OmegaCore.DistTrans`
layer. Preordered distinction strength is presentation structure, not an Alpha
primitive.

Deprecated facade: use `ProtoOmega.Transport.Native` for Alpha-native transport
definitions. This file remains temporarily for compatibility during migration.
-/
