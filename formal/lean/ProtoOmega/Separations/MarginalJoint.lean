import OmegaCore.MarginalJoint

/-!
ProtoOmega.Separations.MarginalJoint

Finite separation showing that marginal-like non-erasure need not imply
strictly joint non-erasure. This facade currently re-exports the checked legacy
`OmegaCore.MarginalJoint` layer.

Deprecated facade: use `ProtoOmega.Separations.MarginalJointNative` for the
Alpha-native finite separation. This file remains temporarily for compatibility
during migration.
-/
